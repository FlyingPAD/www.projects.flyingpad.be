import { ConvertMassPipe } from './convert-mass.pipe';

describe('ConvertMassPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertMassPipe();
    expect(pipe).toBeTruthy();
  });
});
